<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_height="match_parent"
    android:paddingLeft="10dp" android:paddingRight="10dp"
    android:background="#fff" android:layout_width="match_parent"
    android:paddingTop="10dp"
   >
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:scaleType="center"
        android:background="@drawable/authentication"
        android:contentDescription="@string/app_name" />
    <EditText android:layout_height="wrap_content" android:id="@+id/txtUsername"
         android:hint="@string/username"
        android:layout_width="match_parent">
        <requestFocus></requestFocus>
    </EditText>
    <EditText android:layout_height="wrap_content"
        android:inputType="textPassword" android:id="@+id/txtPassword"
        android:fontFamily="sans-serif"
        android:hint="@string/password"
        android:layout_width="match_parent"></EditText>
    <LinearLayout android:layout_width="match_parent"
        android:layout_marginTop="30dp"
        android:layout_height="wrap_content" android:id="@+id/linearLayout1">
        <Button android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:layout_weight="0.5"
            android:id="@+id/btnLogin" android:text="Login"></Button>
        <Button android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:layout_weight="0.5"
            android:id="@+id/btnCancel" android:text="Cancel"></Button>
    </LinearLayout>
</LinearLayout>